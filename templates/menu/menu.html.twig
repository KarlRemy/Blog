<header class="cs-header navbar navbar-expand-lg navbar-dark navbar-floating navbar-sticky py-4" style="height: 5em">
    <div class="container px-0 px-xl-3 respons-nav">
        <button id="side-navbar-btn" class="navbar-toggler ml-n2 mr-2" type="button">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="display-connexion d-flex align-items-center order-lg-3 ml-lg-auto navbar-content">
            {% if app.user %}
                    <a class="btn btn-primary m-2" href="{{ path('app_logout') }}" rel="noopener">
                        <i class="fe-user-check font-size-lg"></i> Déconnexion
                    </a>
            {% else %}
                <a class="btn btn-primary m-2" href="{{ path('app_login') }}" rel="noopener">
                    <i class="fe-user font-size-lg"></i> Connexion
                </a>
            {% endif %}
        </div>

        <div class="cs-offcanvas-collapse order-lg-2 navbar-no-slide">
            <div class="cs-offcanvas-body">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link nav-link-bold" href="#">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link nav-link-bold" href="#">Post</a></li>
                    <li class="nav-item"><a class="nav-link nav-link-bold" href="#">Contact</a></li>
                </ul>
            </div>
        </div>
    </div>
</header>